# HD2CB 项目协作规则

## 项目位置

- Git 根目录为本文件所在目录，默认分支为 `main`，远端为 `https://github.com/JunziSama/HD2CB.git`。
- 应用源码位于 `ChargeBar-win32-x64/resources/app`，Windows 程序启动位置保持 `ChargeBar-win32-x64/ChargeBar.exe`。
- 保持 Electron 4.2.12 / Node 10.11 兼容性；未经明确任务要求不升级运行时。
- 保留原作者版权、ISC 许可证和上游来源说明。

## 已授权的自动 Git 工作流

用户已明确授权：每次完成本项目修改并通过相关验证后，自动提交此次任务的相关改动，再推送 `origin/main`，无需逐次询问。

1. 开始工作前检查当前分支、工作区及远端状态，辨认已有的用户改动。
2. 完成修改后运行与改动相符的检查。代码改动至少运行应用目录中的 `npm test`；涉及运行时、窗口、托盘和退出流程时，在有运行环境的情况下运行 `npm run test:smoke`。
3. 提交前审查 diff 和暂存清单，只纳入当前任务相关文件，不擅自纳入无关或来源不明的用户改动。提交说明准确描述最终变化。
4. 推送前 fetch 并检查远端。若远端前进，在保留用户改动和远端历史的前提下同步、复核及必要时重新验证，再正常推送。
5. 禁止强制推送、覆盖远端历史、自动丢弃用户修改。测试失败、认证失效或冲突无法安全处理时，保留成果并报告具体原因。
6. 任务完成时报告提交编号和推送结果。纯问答且无文件修改时不创建空提交。

## 发布及排除项

- 自动 Git 操作由开发任务完成触发，不定时扫描或盲目提交工作区。
- 新建 Release、打版本标签或改变仓库可见性需要该次任务的明确发布要求；首次 `v1.1.0` 发布已获得授权。
- 不上传依赖目录、Electron 运行时、测试缓存、个人配置、凭据、参考工具及原压缩包到 Git。
- 可运行 Windows ZIP 通过 GitHub Releases 发布，包含完整运行所需文件、许可证与 SHA-256 校验文件。
- 没有真实游戏运行验证时，不得声称已经通过真实 HD2 内测试。
