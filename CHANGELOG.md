# 更新记录

## 1.3.0 — 2026-09-18

- 发布前原生 F2 切换、F3 退出、R 透传及失焦释放通过；真实 HD2 尚未实测。
- 固定 app 源码与 Releases 版本目录，统一版本化构建命名。
- 新增设置内显示模式，热量文字右下对齐且可关闭；退出卡片移至末尾。
- 默认 F1 模式、F2 武器、F3 退出；配置版本 4 仅迁移旧默认组合，保留自定义设置。

- 新增 LAS-17 双刃镰刀累计估算热量、四阶段颜色、独立百分比显示、普通/严寒/自定义参数，以及被动 R 换弹监控和手动归零。
- 配置升级至版本 3，保留旧设置；切出或切换后保留余热继续冷却。新增原生 R 透传、七武器循环、参数持久化和热量状态测试。尚未进行真实游戏校准。
- 新增电弧发射器、净化者、忠诚者，绿色蓄力，满格青色保持到松手，松手清零。
- 六武器按统一列表生成设置和托盘选项，F3 依次循环；保留既有三把完成后延时隐藏的行为及热键修复。
- 新增两类完成行为的状态与定时器回归，验证六武器原生切换、选择持久化和渲染。忠诚者 0.75 秒待游戏内校准。

## 1.2.0 — 2026-09-17

- 新增第三武器 LAS-99 类星体加农炮：3 秒绿色蓄力，满格青色保持 0.5 秒后隐藏；支持设置、托盘和 F3 三武器循环，保留已有配置。

- 修复 F3 回调内重建全局热键造成重复触发、主进程饥饿和托盘无响应；切换延后执行，未变化的绑定保持注册，菜单关闭后再刷新。
- 增加纪元和磁轨炮（不安全模式）的选择、分段颜色、刻度及参数说明。
- 新增默认 F3 切换武器，可独立开关、自定义；支持托盘和设置窗口选择并持久化。
- 三把武器到顶后保持完成提示 0.5 秒（纪元/磁轨炮红色、类星体青色），再隐藏直到下一次点击；取消旧轮次定时器。
- 武器名称在可见进度条旁显示 2 秒；未显示时延后提示，隐藏后结束提示。
- 迁移旧配置保留原设置，避让已有 F3/F4 绑定；验证三项热键冲突和保存失败。
- 增加边界、生命周期、配置迁移及实际渲染检查。未进行真实 HD2 验证。
- Windows 下载包：`HD2CB-v1.2.0-win32-x64.zip`，完整解压后运行 `ChargeBar-win32-x64/ChargeBar.exe`，附 SHA-256 校验文件。

## 1.1.0 — 2026-09-17

基于 nine-sec/HD2-ChargeBar 的首次改进版本。

- 新增常驻托盘、启动提示及单实例限制，重复启动不会再创建另一套应用。
- 新增 Windows 前台窗口检测，仅 HD2 聚焦时显示覆盖层、启用热键；失焦后清空充能状态。
- 支持隐藏、按住右键显示、游戏内常显三种模式，默认按住右键显示。
- 新增中文设置窗口，可独立开关并自定义模式切换和退出热键，保存设置并在重启后恢复。
- 新增检测失败提示、重新检测和助手退出清理。
- 保留原有进度条样式、3 秒计时和 2/2.5 秒颜色阈值。
- 修复 Electron 4 / Node 10 的启动与目录创建兼容问题，避免 iohook 退出卸载崩溃。

### 下载与使用

下载 `HD2CB-v1.1.0-win32-x64.zip`，完整解压后运行 `ChargeBar-win32-x64/ChargeBar.exe`。通过托盘设置显示模式和热键。默认 F1 切换模式、F2 退出，仅在 HD2 前台时生效。

`SHA256SUMS.txt` 提供运行包的 SHA-256 校验值。GitHub 自动提供的 Source code 压缩包仅含源码。

### 验证与已知限制

状态、配置及检测助手测试通过；实际 Electron 启动、三次启动单实例、设置保存与恢复、配置损坏回退、助手异常恢复和退出清理通过。

**真实 HD2 内的焦点切换、200 毫秒隐藏目标和覆盖效果尚未实测。** 建议使用游戏窗口化或无边框模式，独占全屏及真实游戏输入需要进一步确认。

程序显示鼠标长按时间，不读取武器的实际充能状态。游戏反作弊兼容性没有保证。